{
  "name": "desktop-sticky-notes",
  "version": "0.1.0",
  "private": true,
  "description": "An Obsidian desktop sticky-notes plugin.",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "obsidian": "latest",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@electron/remote": "^2.1.2"
  }
}
